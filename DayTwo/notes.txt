setInterval() -> Method in JS that dictates after how long after
how long a function runs
USAGE
..........setInterval(NameofFunction, timeinmiliseconds)

Date() -> Method in JS that returns the exact date and time of
your current location.
USAGE
....... const now = new Date()

document.querySelector() -> Method in JS that returns the first
element within the document that matches the specified selector, 
or group selectors.

style.transform -> Allows you to rotate, scale, move, skew elements.
USAGE
....... object.style.transform = "none|transform-functions|initial|inherit"